(define (loop [n : Int]) : (Int -> ((Int -> Int) -> Int))
  (lambda ([acc : Int])
    (lambda ([f : (Int -> Int)])
      (if (= n 0)
          acc
          (((loop (- n 1)) (f acc)) f)))))

(define (id [x : Int]) : Int
  x)

(define (benchmark) : Unit
  (print-int (((loop (read-int)) 0) id)))
(executable
  (name main)
  (modules main)
  (libraries lambda_S_dti))

(executable
 (name bench)
 (modules bench)
 (libraries lambda_S_dti yojson core_bench core_unix.command_unix))

(env
 (dev
  (flags (:standard -w -3-6))))

(install
  (section bin)
  (files (main.exe as lSdti)))
